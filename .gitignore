# Generated by tree-sitter generate (keep src/parser.c in version control)
src/grammar.json
src/node-types.json

# Build artifacts
build/
target/
*.dylib
*.dll

# Compiled Neovim parser (built locally by `make`)
parser/*.so

# Node
node_modules/

# Rust
Cargo.lock
